<%= bootstrap_form_for(@galeri_foto) do |f| %>
  <% if galeri_foto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(galeri_foto.errors.count, "error") %> mengakibatkan Galeri Foto tidak dapat disimpan:</h2>

      <ul>
      <% galeri_foto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :judul %>
  <%= f.text_field :keterangan %>
  <%= f.text_field :tautan %>
  <%= f.submit "Membuat Galeri Foto", :class => "btn btn-primary" %>
<% end %>